// TODO Fase 5: Workbox Service Worker - cache assets, offline
// Placeholder until Workbox build is configured
self.addEventListener('install', () => self.skipWaiting());
self.addEventListener('activate', () => self.clients.claim());
